const array1 = [0, 1, 1, 5, "ninja"];

console.log(array1.includes("grant"));
// => false

//it works!

console.log(array1.indexOf("ninja") !== -1);
// => true
//it works

//This shows how indexOf as an operand can be used to determine if an array contains an element or not.
