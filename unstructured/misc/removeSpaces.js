
// a walk in the park // awalkinthepark
const removeSpaces = (string) => {
    console.log('string', string)
    let array = string.split(" ");
    console.log(array)
    console.log(array.join(""))
    
  
}

removeSpaces('a walk in the park')